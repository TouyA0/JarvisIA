# brain/ — dépendances du serveur central (Phase 1)
# Séparé de requirements.txt (agent desktop) : le brain tourne comme process
# à part, même si aujourd'hui les deux vivent sur la même machine.

fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
websockets==13.1
python-multipart==0.0.12
requests==2.31.0
cryptography==43.0.1
tzdata==2024.2
pypdf==6.11.0
psutil==5.9.8
trafilatura==2.2.0
adb-shell==0.4.4
zeroconf==0.151.3
